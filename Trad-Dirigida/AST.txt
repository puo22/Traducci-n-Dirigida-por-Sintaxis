Ejemplo de AST decorado para: a + b * c

Árbol con atributos (como en el PDF, pág. 28):

E
├─ E₁ → trad = "a"
├─ + 
└─ T → trad = "mul(b, c)"
     ├─ T₁ → trad = "b"
     ├─ *
     └─ F → trad = "c"

Resultado final:
E.trad = "suma(a, mul(b, c))"

Este AST se genera mediante el ETDS y se imprime como traducción.
